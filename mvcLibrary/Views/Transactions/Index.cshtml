@using mvcLibrary.Models.Entity
@model List<TblProcess>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<h2>Returned Book List</h2>
<table class="table table-bordered">
    <tr>
        <th>Process ID</th>
        <th>Book Name</th>
        <th>Member</th>
        <th>Employee</th>
        <th>Borrow Date</th>
        <th>Returned Date</th>
        <th>Details</th>
    </tr>
    @foreach (var b in Model)
    {

        <tr>
            <td>@b.ID</td>
            <td>@b.TblBook.Name</td>
            <td>@b.TblMembers.Name&nbsp;@b.TblMembers.LastName</td>
            <td>@b.TblEmployee.Employee</td>
            <td>@Convert.ToDateTime(b.BorrowDate).ToString("dd/MM/yyyy")</td>
            <td>@Convert.ToDateTime(b.ReturnedDate).ToString("dd/MM/yyyy")</td>

            <td><a href="/Borrow/BorrowReturn/@b.ID" class="btn btn-default" style="background-color:#00ffff">Details</a></td>


        </tr>

    }

</table>

